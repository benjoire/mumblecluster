MumbleCluster

This is the master GitHub repository for MumbleCluster – a modular compute cluster, creative ecosystem, and memory-anchored ops framework designed by Ben & Deniz.

Structure:
- 'syncstream/' → Protocol-driven coordination layer (YAML, ODS, logs)
- 'sandbox/' → Experimental pods, drafts, unversioned R&D
- 'production/' → Stable and tested modules for deployment

Access:
SSH-authenticated access only. Contact 'benjoire' for deployment roles or collab tokens.

Protocols:
We use a strict emoji command grammar.
See 'syncstream/auxiliaries' for reference.

Status:
- Active branch: 'main'
- Core ops file: 'SYNCSTREAM_sheet.ods'

CREDO:
Big-O ~ 🧠::brain <- Deniz <-> Ben -> 🪖::helmet + you (hope-des)
